# Auto detect text files and perform LF normalization
* text=auto

# TouchDesigner files are binary — never normalise these
*.toe binary
*.tox binary
*.tdc binary

# Media / geometry
*.fbx binary
*.mov binary
*.mp4 binary
